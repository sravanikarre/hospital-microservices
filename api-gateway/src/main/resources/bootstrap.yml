spring:
  application:
    name: api-gateway

  cloud:
    config:
      uri: http://localhost:8888    # URL of config server
      fail-fast: true               # Optional, to crash if config not loaded

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
